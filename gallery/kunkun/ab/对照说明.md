# KUNKUN · 两种制图方式对照

本次使用此前交付的 KUNKUN 几何规整稿。没有重新描摹源图，也没有改变图形或字形。A/B 均保留相同的 3 个图形部件和 6 个字母路径、位置及显示尺寸。

- A：路径结构图。显示直线和圆弧的节点，以及真实圆心到弧端点的半径。该稿没有 Bézier 曲线，因此不添加虚构的控制柄。
- B：比例定位图。使用长斜向平行参考线、图形高度线、对齐线、完整构造圆、灰色排线和图上尺寸；字标保留独立的字高与间距定位。

本次定义 x = 斜边法向间隙 = 60 u，因此主笔画法向宽度为 5x，大圆角半径为 2.5x。51°、圆角参数和字标排布继承自先前规整稿，不声称是原图作者的规范。模数选择只改变标注，不改变成品路径。

已从两张生成的 SVG 中提取 9 条成品路径及各字母变换，与原规整 SVG 逐一核对，完全一致。已检查 9 段主体圆弧端点与构造圆对应、主笔画宽度与间隙的法向距离。skill 文件未修改。此验证针对制图引用关系，不重做原稿设计验收。

文件：AB-comparison 为并排图；A-path-structure 和 B-proportion-guides 为单独页面，均有 PNG 和 SVG；logo-unchanged.svg 是同一规整成品；geometry.json 为既有参数副本；verification.json 为检查结果。
