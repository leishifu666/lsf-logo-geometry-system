name: Skill tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install -r skills/lsf-logo-geometry-system/requirements.txt
      - run: python -m unittest discover -s skills/lsf-logo-geometry-system/tests -v
